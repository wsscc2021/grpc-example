## nodejs's grpc quick start

- install required library
  ```
  npm install --save @grpc/grpc-js
  npm install --save @grpc/proto-loader
  ```

- execute grpc server
  ```
  node helloworld_server.js
  ```

- execute grpc client
  ```
  node helloworld_client.js
  ```